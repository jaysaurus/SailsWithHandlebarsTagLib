<!DOCTYPE HTML PUBLIC "-//W3C//DTD HTML 4.01 Transitional//EN" "http://www.w3.org/TR/html4/loose.dtd">
<html lang="en">
<head>
    <meta http-equiv="content-type" content="text/html; charset=utf-8">
    <title>Best website ever</title>
    <style>
        .box {
            border:1px solid #ccc;
            padding: 5px;
            margin: 10px;
        }
    </style>
</head>
<body>
    <h1>My Amazing Sample Website</h1>
    {{#if (and (eq bar.name 'Little Bar')
               (ne foo.name 'Pants Man')) }}
    <p>I am the product of an "if" condition using embedded operands!</p>
    <div class="box">
        <p>Hi! my name is: {{foo.name}}</p>
        <p>I was created on: {{foo.createdAt}}</p>
        <p>My child is: {{#with (first foo.bars)}}{{name}}{{/with}}</p>
        <div class="box">
            <p>Hi! my name is: {{bar.name}}</p>
            <p>I am a child of: {{bar.foo.name}}</p>
        </div>
    </div>

    <p>{{toUpperCase 'This writing has been set to upper case by a helper'}}</p>
    {{else}}
        I am the product of an "else" condition
    {{/if}}
</body>
</html>
